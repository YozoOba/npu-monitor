# NPU 集群监控使用指导

## 1. 组件和部署边界

项目现在包含三个互相独立的组件：

| 组件 | 目录 | 职责 | 是否需要 NPU 环境 |
|---|---|---|---|
| Agent | `agent/` | 执行 `npu-smi info`、本地保存、断线排队、主动上报 | 是 |
| Collector | `collector/` | 接收、准确性校验、SQLite 去重落库、生成基础汇聚数据 | 否 |
| Console | `console/` | 命令行查询、JSON 查询接口、Web 当前状态和历史趋势 | 否 |

`cluster_common/` 只定义 Agent 与 Collector 共同使用的版本化数据协议。Collector 不包含页面、节点排行等业务展示代码。Console 通过 Collector 的中性快照和序列导出接口读取数据，可以独立部署、停止和升级。

项目根目录的 `npu_monitor.py`、`start.sh`、`stop.sh` 等是上一版单机模式，继续保留用于兼容；集群部署应使用三个新目录。

## 2. 数据链路

每轮采集遵循以下顺序：

1. Agent 执行 `npu-smi info`；
2. Agent 将结果写入本机 CSV 和采样状态 JSONL；
3. Agent 将同一采样原子写入本机 `spool`；
4. 独立上传线程向 Collector POST；
5. Collector 在单个 SQLite 事务中写入采样和卡数据；
6. Collector 根据 `node_id + collected_at` 去重并更新集群快照；
7. Console 从 Collector 读取快照或时间序列并负责展示。

网络中断不会停止采集。待发送数据保存在 Agent 数据卷中，恢复后按采样时间补传。重复补传返回成功，但不会重复计数；相同节点、相同时间却包含不同测量值时返回 HTTP 409，并由 Agent 移入 `rejected` 目录等待人工核对。

## 3. 运行要求

- 每台 NPU 机器可以直接复用本机已有的 `vllm-ascend` 镜像，不要求镜像版本一致，也不要求重新构建项目镜像；
- 用于 Agent 的现有镜像必须包含 Python 3.6+，宿主机必须提供 `npu-smi`、NPU 设备及驱动目录；
- Collector 和 Console 只需要 Python 3.6+ 标准库；
- 不需要安装第三方 Python 包，也不需要安装 SQLite 服务；Python 标准库 `sqlite3` 直接操作单个数据库文件；
- 所有容器必须通过 `docker run --init` 启动；
- Agent 到 Collector 的 TCP 端口必须可达，默认端口为 `18080`；
- 每台机器必须分配长期不变且全局唯一的 `node_id`；
- 建议所有物理机使用相同 NTP/Chrony 时间源。

先确认本机镜像存在：

```bash
docker image inspect 13315b656180 >/dev/null
```

容器创建后再在容器内确认：

```bash
python3 --version
python3 -c "import sqlite3; print(sqlite3.sqlite_version)"
npu-smi info
```

最后一条只要求 Agent 所在容器成功。

## 4. 复用现有镜像创建 NPU Monitor 容器

这是推荐方式。项目不依赖 Docker Compose，也不要求执行 `docker build`。

先把项目放在每台 NPU 宿主机上，例如：

```bash
cd /work/monitor
chmod +x deploy/create_npu_monitor_container.sh
```

只创建容器、暂不启动 Agent：

```bash
./deploy/create_npu_monitor_container.sh 13315b656180 /work/monitor
```

脚本的两个位置参数分别是：

1. 本机已有的镜像 ID 或镜像名；
2. 宿主机上的项目目录，该目录会挂载到容器的 `/work/monitor`。

默认容器名是 `npu-monitor`。如需修改容器名或共享内存大小：

```bash
export NPU_MONITOR_CONTAINER_NAME=npu-monitor-node01
export NPU_MONITOR_SHM_SIZE=500g
./deploy/create_npu_monitor_container.sh 13315b656180 /work/monitor
```

脚本会沿用当前 8 卡 910B 环境所需的设备和驱动挂载，包括：

- `/dev/davinci0` 至 `/dev/davinci7`；
- `/dev/davinci_manager`、`/dev/devmm_svm`、`/dev/hisi_hdc`；
- `dcmi`、`hccn_tool`、`npu-smi`、驱动库及 Ascend 配置；
- 宿主机 `/mnt`；
- 项目目录到容器 `/work/monitor`。

创建前会检查镜像、项目文件、设备和挂载源是否存在。已有同名容器时会直接报错，不会自动删除旧容器。脚本始终添加 `--init`，由 Docker init 进程负责回收退出的子进程。

创建后检查：

```bash
docker ps --filter name=npu-monitor
docker exec npu-monitor npu-smi info
docker exec npu-monitor python3 --version
docker exec -it npu-monitor bash
```

### 4.1 创建后直接运行 Agent

Collector 地址是节点运行参数，不能从镜像 ID 和目录推断。创建容器前设置 Collector 地址后，脚本会让 Agent 成为容器主进程；Docker 重启容器后 Agent 也会自动重新启动：

```bash
cd /work/monitor
export NPU_AGENT_COLLECTOR_URL=http://192.168.10.20:18080
export NPU_AGENT_NODE_ID=npu-node-01
export NPU_AGENT_NODE_NAME=910b-server-01

./deploy/create_npu_monitor_container.sh 13315b656180 /work/monitor
```

`NPU_AGENT_NODE_ID` 未设置时默认使用宿主机短主机名，但生产环境建议明确设置并保持不变。Agent 数据保存在宿主机项目目录的 `runtime-data/agent/`，更换容器不会丢失待补传队列和上传健康状态。

检查：

```bash
docker logs --tail 100 npu-monitor
docker exec npu-monitor python3 -m agent.healthcheck
find /work/monitor/runtime-data/agent -maxdepth 2 -type f | sort
```

Agent 数据目录包含：

```text
runtime-data/agent/
├── daily/stats_YYYY-MM-DD.csv
├── sample_status/samples_YYYY-MM-DD.jsonl
├── spool/                 # 尚未成功上报
├── rejected/              # Collector 永久拒绝或损坏的记录
├── health.json            # 本机采集健康
└── upload_health.json     # 上报健康和积压数
```

`upload_health.json` 中的上传状态会跨 Agent 重启恢复，包括：

- `last_success` 和 `last_failure_at`；
- `upload_unavailable_since`；
- `consecutive_failures`、`total_successes` 和 `total_failures`；
- `pending_samples`、`oldest_pending_at` 和 `oldest_pending_age_seconds`。

告警建议优先使用“存在积压且最老积压年龄超过阈值”，而不是只依赖重试次数：

```text
pending_samples > 0 AND oldest_pending_age_seconds > alert_threshold
```

## 5. 启动主节点 Collector

Collector 不需要 NPU，可以直接在主节点已有的 Python 3.6+ 环境或普通现有容器中运行。下面是不构建镜像、直接从项目目录启动的方式：

```bash
cd /work/monitor
export PYTHONPATH=/work/monitor
export NPU_COLLECTOR_DATA_DIR=/work/monitor/runtime-data/collector
export NPU_COLLECTOR_PORT=18080
mkdir -p "$NPU_COLLECTOR_DATA_DIR"
python3 -u -m collector.app
```

Collector 应作为容器主进程或由现有的进程管理系统守护，不建议使用 `nohup ... &`。在另一个终端检查：

```bash
curl -sS http://127.0.0.1:18080/health
```

Collector 数据卷包含：

```text
runtime-data/collector/
├── cluster.sqlite3
├── cluster.sqlite3-wal
├── cluster.sqlite3-shm
└── latest_snapshot.json
```

不要在 Collector 运行时只复制 `cluster.sqlite3` 文件作为备份，因为 WAL 中可能仍有已提交数据。应先停止容器后复制整个目录，或使用 SQLite 在线备份命令。

## 6. 启动独立 Console

Console 可以放在主节点，也可以放在任何能够访问 Collector 的普通节点：

```bash
export NPU_CONSOLE_COLLECTOR_URL=http://192.168.10.20:18080
export PYTHONPATH=/work/monitor
export NPU_CONSOLE_PORT=18081
python3 -u -m console.web
```

浏览器访问：

```text
http://Console节点IP:18081/
```

命令行查询不要求启动 Web 容器。只要当前目录包含代码，即可执行：

```bash
python3 -m console.cli \
  --collector-url http://192.168.10.20:18080 summary

python3 -m console.cli \
  --collector-url http://192.168.10.20:18080 nodes --state offline

python3 -m console.cli \
  --collector-url http://192.168.10.20:18080 history --hours 24 --bucket 300

python3 -m console.cli \
  --collector-url http://192.168.10.20:18080 history --hours 168 --bucket 3600 --node npu-node-01
```

## 7. 状态含义和计算口径

节点状态：

- `online`：最新采样在新鲜阈值内且卡数完整；
- `degraded`：节点仍在上报，但最新采样缺卡或采集失败；
- `stale`：超过约 2 个采集周期没有新采样；
- `offline`：超过约 5 个采集周期没有新采样。

阈值同时受 Collector 的最小阈值控制，因此不会因为 Agent 错误上报极小采集周期而频繁抖动。

计算口径：

- 当前集群利用率：所有 `online/degraded` 节点最新有效卡的利用率总和除以有效卡数；
- 不对“每台节点平均值”再次平均，避免缺卡节点获得错误权重；
- 登记容量：所有已登记节点的预期卡数，不因节点 stale/offline 而消失；
- 新鲜样本数：仅统计 `online/degraded` 节点最新采样中的有效卡；
- 舰队新鲜度：新鲜卡样本数除以全部登记容量，stale/offline 节点会降低该值；
- 上报完整度：新鲜卡样本数除以当前仍在上报节点的预期卡数；没有上报节点时显示 `N/A`；
- 最近已知卡数：所有节点最新一次采样中包含的卡数，仅用于解释容量状态，不参与实时利用率；
- 历史利用率：时间桶内所有有效卡样本加权平均；
- 历史覆盖率：时间桶内实际卡样本总数除以预期卡样本总数，失败采样也进入分母；
- HBM 百分比：所有具有完整 HBM 数据的卡使用量总和除以容量总和；
- 默认 `busy` 为利用率不低于 80%，`idle` 为不高于 10%。

Collector 会把所有时间统一为 UTC。Console 当前也显示 UTC，便于多节点去重和时间线一致；需要本地时区展示时只修改 Console，不改变存储协议。

stale/offline 节点的历史卡数据不会参与当前利用率。Console 会将这类节点的新鲜利用率显示为 `-`，同时单独保留“登记容量”和“最近已知卡数”，避免把“数据过期”误读成“机器实际没有 NPU”。

## 8. 配置项

### Agent

| 环境变量 | 默认值 | 说明 |
|---|---:|---|
| `NPU_AGENT_NODE_ID` | 必填 | 稳定且唯一的节点编号 |
| `NPU_AGENT_NODE_NAME` | 主机名 | 展示名称 |
| `NPU_AGENT_COLLECTOR_URL` | 必填 | Collector 地址 |
| `NPU_AGENT_EXPECTED_CARDS` | 8 | 预期卡数 |
| `NPU_AGENT_COLLECT_INTERVAL` | 60 | 采集周期，秒 |
| `NPU_AGENT_COMMAND_TIMEOUT` | 10 | `npu-smi` 超时 |
| `NPU_AGENT_HTTP_TIMEOUT` | 5 | 单次 HTTP 超时 |
| `NPU_AGENT_RETENTION_DAYS` | 180 | 本地 CSV、状态及拒绝记录保留天数 |
| `NPU_AGENT_SPOOL_RETENTION_DAYS` | 7 | 未上传队列最大保留天数 |
| `NPU_AGENT_SPOOL_MAX_FILES` | 20000 | 队列文件数上限 |
| `NPU_AGENT_SPOOL_MAX_BYTES` | 536870912 | 队列字节上限 |
| `NPU_AGENT_UPLOAD_BATCH_SIZE` | 100 | 每轮最多补传条数 |
| `NPU_AGENT_MIN_FREE_BYTES` | 104857600 | 本地最小剩余空间 |
| `NPU_AGENT_MIN_FREE_INODES` | 1000 | 本地最小剩余 inode |
| `NPU_AGENT_NPU_SMI_BIN` | `npu-smi` | 测试时可指定假命令 |

### Collector

| 环境变量 | 默认值 | 说明 |
|---|---:|---|
| `NPU_COLLECTOR_PORT` | 18080 | 容器监听端口 |
| `NPU_COLLECTOR_RETENTION_DAYS` | 180 | 集群历史保留天数 |
| `NPU_COLLECTOR_STALE_SECONDS` | 120 | stale 最小阈值 |
| `NPU_COLLECTOR_OFFLINE_SECONDS` | 300 | offline 最小阈值 |
| `NPU_COLLECTOR_MAX_FUTURE_SECONDS` | 300 | 允许的最大未来时间偏差 |
| `NPU_COLLECTOR_CLOCK_SKEW_WARN_SECONDS` | 30 | 时钟偏差警告阈值 |
| `NPU_COLLECTOR_BUSY_UTILIZATION` | 80 | 高负载卡阈值 |
| `NPU_COLLECTOR_IDLE_UTILIZATION` | 10 | 空闲卡阈值 |
| `NPU_COLLECTOR_MIN_FREE_BYTES` | 209715200 | Collector 最小剩余空间 |
| `NPU_COLLECTOR_MIN_FREE_INODES` | 1000 | Collector 最小剩余 inode |

### Console

| 环境变量 | 默认值 | 说明 |
|---|---:|---|
| `NPU_CONSOLE_COLLECTOR_URL` | `http://127.0.0.1:18080` | Collector 地址 |
| `NPU_CONSOLE_PORT` | 18081 | Console Web 端口 |
| `NPU_CONSOLE_HTTP_TIMEOUT` | 10 | 访问 Collector 的超时 |

## 9. 停止、重启和升级

```bash
docker stop -t 15 npu-monitor
docker start npu-monitor
```

使用第 4.1 节方式创建时，Agent 是容器主进程并使用 `--init`，不使用 PID 文件、`nohup` 或后台 Python 子进程。`docker stop` 会停止 Agent；`docker start` 会重新启动 Agent，并保留宿主机 `runtime-data/agent/` 中的数据。Agent 上传使用线程，因此不会因每次 HTTP 请求制造额外 Python 进程。

删除容器不会删除项目目录中的运行数据，但下面的操作仍应由人工明确执行，创建脚本不会自动覆盖同名容器：

```bash
docker stop -t 15 npu-monitor
docker rm npu-monitor
```

升级代码或更换已有镜像后，用相同 `NPU_AGENT_NODE_ID` 再次执行创建脚本。不要修改已部署节点的节点 ID，否则会被识别为一台新机器。项目保留的 Dockerfile 和 `deploy/build_images.sh` 仅作为需要固定运行环境时的可选方式，不是本项目的默认部署路径。
