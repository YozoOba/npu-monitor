# NPU 集群监控使用指导

本文按实际使用场景组织。项目默认直接复用服务器上已有的 `vllm-ascend` 镜像，不要求 Docker Compose，也不要求重新制作 Agent、Collector、Console 镜像。

## 1. 先选择你的使用场景

| 场景 | 适用目的 | 推荐章节 |
|---|---|---|
| 一台 910B 机器完成全链路验证 | 当前测试、功能验收 | 第 3 节 |
| 十几台机器正式组成集群 | 长期运行 | 第 4 节 |
| 已经进入某个现有容器 | 临时手动启动或排查 Agent | 第 5 节 |
| 日常启停、升级和故障检查 | 运维 | 第 6、7 节 |

正式集群中的角色分配是：

- 每台 NPU 机器运行一个 Agent；
- 主节点运行一个 Collector；
- Console 通常也放在主节点，但可以放在任何能访问 Collector 的机器；
- 三个组件可以复用同一个已有镜像。创建三个容器不等于制作三个镜像。

## 2. 所有场景的共同准备

### 2.1 运行条件

- 项目代码放在宿主机 `/work/monitor`；
- Agent 所在容器需要 Python 3.6+，并且能够执行 `npu-smi info`；
- Collector 和 Console 只需要 Python 3.6+ 标准库；
- 不需要安装第三方 Python 包；
- 不需要安装 SQLite 服务，Python 自带的 `sqlite3` 模块直接读写数据库文件；
- Agent 必须能访问主节点 TCP `18080` 端口；
- 浏览器需要能访问 Console 所在机器 TCP `18081` 端口；
- 每台 Agent 必须使用长期不变、全局唯一的 `NPU_AGENT_NODE_ID`；
- 所有物理机应使用相同的 NTP 或 Chrony 时间源；
- 所有长期运行的容器都由项目内置的 `deploy/mini_init.py` 作为 PID 1；不依赖 Docker `--init`；
- Docker 运行日志按单文件 20 MB、最多 5 个文件轮转，可通过环境变量调整。

### 2.2 基础检查

在宿主机确认镜像和代码：

```bash
cd /work/monitor
docker image inspect 13315b656180 >/dev/null
test -f agent/app.py
test -f collector/app.py
test -f console/web.py
```

在 NPU 容器内确认运行环境：

```bash
cd /work/monitor
python3 --version
python3 -c "import sqlite3; print(sqlite3.sqlite_version)"
npu-smi info
```

## 3. 场景一：在一台 910B 机器上完成全链路验证

该场景在同一台机器上创建三个容器，但三者复用同一个现有镜像，不会产生三个新镜像。组件都由 mini-init 守护，测试方式与正式部署一致。

### 3.1 准备脚本

```bash
cd /work/monitor
chmod +x deploy/*.sh
export IMAGE_ID=13315b656180
```

### 3.2 启动 Collector

```bash
./deploy/create_collector_container.sh "$IMAGE_ID" /work/monitor
curl -sS http://127.0.0.1:18080/health
```

### 3.3 启动 Console

```bash
export NPU_CONSOLE_COLLECTOR_URL=http://127.0.0.1:18080
./deploy/create_console_container.sh "$IMAGE_ID" /work/monitor
curl -sS http://127.0.0.1:18081/health
```

### 3.4 启动 Agent

```bash
export NPU_AGENT_NODE_ID=test-node-01
export NPU_AGENT_NODE_NAME=test-910b-01
export NPU_AGENT_CLUSTER_ID=test-cluster
export NPU_AGENT_COLLECTOR_URL=http://127.0.0.1:18080
./deploy/create_agent_container.sh "$IMAGE_ID" /work/monitor
```

Agent 脚本会动态挂载宿主机现有的 `/dev/davinciN` 设备、Ascend 驱动、`npu-smi`、`/mnt` 和项目目录，并使用 `--net=host`、`--privileged=true` 及 `--restart unless-stopped`。HCCL/HCCN 文件默认自动判断：检测到 310P 时跳过；其他型号仅在宿主机同时存在 `hccn_tool` 和 `/etc/hccn.conf` 时挂载，因此910B/910C保持原有行为。

如需覆盖自动判断，可以在创建容器前设置：

```bash
# 310P 或明确不需要 HCCL
export NPU_AGENT_HCCL_MOUNTS=disabled

# 910B/910C 且明确要求 HCCL 文件；缺少文件时立即报错
export NPU_AGENT_HCCL_MOUNTS=enabled
```

进入容器排查时使用：

```bash
docker exec -it npu-monitor-agent bash
```

### 3.5 验证全链路

在宿主机另开终端执行：

```bash
curl -sS http://127.0.0.1:18080/health
curl -sS http://127.0.0.1:18081/health
curl -sS http://127.0.0.1:18081/api/snapshot
docker exec npu-monitor-agent python3 -m agent.healthcheck
docker exec npu-monitor-agent ps -o pid,ppid,stat,args -p 1
```

浏览器访问：

```text
http://服务器IP:18081/
```

通过标准：Collector 和 Console 健康检查成功、Agent 识别 8 张卡、Console 出现 `test-node-01`，且 Agent 数据目录持续生成 CSV、状态和上传健康文件。跨入当前配置时区的下一个日期后还会生成月度 XLSX。

## 4. 场景二：部署正式集群

假设主节点 IP 为 `192.168.10.20`，Collector 使用 `18080`，Console 使用 `18081`。正式部署顺序为：先主节点 Collector，再 Console，最后逐台启动 Agent。

### 4.1 主节点启动 Collector

Collector 不需要 NPU。直接复用主节点已有的任意 Python 3.6+ 镜像：

```bash
cd /work/monitor
chmod +x deploy/*.sh
./deploy/create_collector_container.sh 13315b656180 /work/monitor
```

检查：

```bash
docker logs --tail 100 npu-monitor-collector
curl -sS http://127.0.0.1:18080/health
```

Collector 数据保存在宿主机：

```text
/work/monitor/runtime-data/collector/
├── cluster.sqlite3
├── cluster.sqlite3-wal
├── cluster.sqlite3-shm
├── archive/
│   └── cluster-archive.sqlite3  # 超过热窗口的采样、卡明细和已恢复告警
├── backups/                     # 数据管理操作执行前的自动校验备份
└── latest_snapshot.json
```

### 4.2 主节点启动 Console

Console 是无状态服务，可以继续复用同一个镜像：

```bash
cd /work/monitor
export NPU_CONSOLE_COLLECTOR_URL=http://127.0.0.1:18080
./deploy/create_console_container.sh 13315b656180 /work/monitor
```

检查：

```bash
docker logs --tail 100 npu-monitor-console
curl -sS http://127.0.0.1:18081/health
```

### 4.3 每台 NPU 机器启动 Agent

每台机器都可以使用自己本地已有的 `vllm-ascend` 镜像，镜像版本不要求一致。只要镜像中 Python 和 `npu-smi` 工作正常即可。

在第一台 NPU 机器执行：

```bash
cd /work/monitor
export NPU_AGENT_COLLECTOR_URL=http://192.168.10.20:18080
export NPU_AGENT_NODE_ID=npu-node-01
export NPU_AGENT_NODE_NAME=910b-server-01
export NPU_AGENT_CLUSTER_ID=training-a
./deploy/create_agent_container.sh 13315b656180 /work/monitor
```

在其他机器修改节点编号和本机镜像 ID，例如：

```bash
export NPU_AGENT_COLLECTOR_URL=http://192.168.10.20:18080
export NPU_AGENT_NODE_ID=npu-node-02
export NPU_AGENT_NODE_NAME=910b-server-02
export NPU_AGENT_CLUSTER_ID=training-a
./deploy/create_agent_container.sh 本机镜像ID /work/monitor
```

设置了 `NPU_AGENT_COLLECTOR_URL` 后，创建脚本会让 Agent 成为容器主进程。容器重启时 Agent 会自动恢复，不需要再进入容器手动执行 Python。

逐台检查：

```bash
docker logs --tail 100 npu-monitor-agent
docker exec npu-monitor-agent python3 -m agent.healthcheck
curl -sS http://192.168.10.20:18080/health
```

Agent 数据保存在各自宿主机：

```text
/work/monitor/runtime-data/agent/
├── daily/                 # 按当前局点时区保存的每日卡数据 CSV
├── monthly/               # 每节点每月一个 XLSX，每个完成日期一个 Sheet
├── sample_status/         # 每次采集状态
├── spool/                 # 尚未成功上传的样本
├── rejected/              # Collector 永久拒绝的样本
├── archive/               # 超过热数据期限的 CSV、状态、XLSX 和拒绝样本
├── health.json            # 本机采集健康
└── upload_health.json     # 上传健康和积压情况
```

月度文件命名为：

```text
/work/monitor/runtime-data/agent/monthly/节点名称_YYYY-MM.xlsx
```

Agent 在每天第一次采集时，将前一个已完成局点日期的数据补充到月度工作簿。每个 Sheet 顶部包含节点名称、节点 ID、集群 ID、日期和实际时区，逐卡时间也转换到该时区。默认 `NPU_MONITOR_TIMEZONE=auto`，容器创建脚本会挂载宿主机 `/etc/localtime` 和可用的 `/etc/timezone`；也可按局点设为 `UTC`、`UTC+08:00` 等固定偏移。文件名中的斜杠、空格等不安全字符会替换为下划线。XLSX 由 Python 标准库生成，不需要安装 `openpyxl`；生成时会从每日 CSV 原子重建当月文件，因此 Agent 每分钟采集时不会反复改写 XLSX。升级前已经存在的带时区 CSV 会按每行时间戳重新归入当前配置时区对应的 Sheet，旧的 `stats_YYYY-MM.xlsx` 不会被自动删除。

`NPU_AGENT_RETENTION_DAYS` 是热目录窗口，不是删除期限。默认超过 180 天的文件会分别转入 `archive/daily/`、`archive/sample_status/`、`archive/monthly/` 和 `archive/rejected/`，归档文件不会被程序自动删除。

## 5. 场景三：已经进入现有容器，手动启动组件

### 5.1 手动启动 Agent

适合临时测试和排障：

```bash
cd /work/monitor
export PYTHONPATH=/work/monitor
export NPU_AGENT_NODE_ID=npu-node-01
export NPU_AGENT_NODE_NAME=910b-server-01
export NPU_AGENT_COLLECTOR_URL=http://主节点IP:18080
export NPU_AGENT_DATA_DIR=/work/monitor/runtime-data/agent
mkdir -p "$NPU_AGENT_DATA_DIR"
python3 -u -m agent.app
```

### 5.2 手动启动 Collector

```bash
cd /work/monitor
export PYTHONPATH=/work/monitor
export NPU_COLLECTOR_DATA_DIR=/work/monitor/runtime-data/collector
export NPU_COLLECTOR_PORT=18080
mkdir -p "$NPU_COLLECTOR_DATA_DIR"
python3 -u -m collector.app
```

### 5.3 手动启动 Console

```bash
cd /work/monitor
export PYTHONPATH=/work/monitor
export NPU_CONSOLE_COLLECTOR_URL=http://主节点IP:18080
export NPU_CONSOLE_PORT=18081
python3 -u -m console.web
```

这些命令均以前台方式运行。按 `Ctrl+C` 或退出终端后进程会停止，因此只用于测试和排障；正式 Agent 使用第 4.3 节方式启动。

## 6. 日常运维

### 6.1 查看状态和日志

```bash
docker ps --filter name=npu-monitor
docker logs --tail 100 npu-monitor-agent
docker logs --tail 100 npu-monitor-collector
docker logs --tail 100 npu-monitor-console

docker exec npu-monitor-agent python3 -m agent.healthcheck
curl -sS http://主节点IP:18080/health
curl -sS http://主节点IP:18081/health
```

三个创建脚本都使用 Docker `json-file` 日志驱动，默认单文件 20 MB、最多 5 个文件。检查实际配置：

```bash
docker inspect -f '{{json .HostConfig.LogConfig}}' npu-monitor-agent
docker inspect -f '{{json .HostConfig.LogConfig}}' npu-monitor-collector
docker inspect -f '{{json .HostConfig.LogConfig}}' npu-monitor-console
```

### 6.2 停止和启动

Agent 节点：

```bash
docker stop -t 30 npu-monitor-agent
docker start npu-monitor-agent
```

主节点：

```bash
docker stop -t 30 npu-monitor-console npu-monitor-collector
docker start npu-monitor-collector npu-monitor-console
```

### 6.3 删除和重建 Agent 容器

创建脚本不会自动删除或覆盖同名容器。升级代码或更换本机镜像时执行：

```bash
docker stop -t 30 npu-monitor-agent
docker rm npu-monitor-agent

export NPU_AGENT_COLLECTOR_URL=http://主节点IP:18080
export NPU_AGENT_NODE_ID=原来的节点ID
export NPU_AGENT_NODE_NAME=原来的节点名称
./deploy/create_agent_container.sh 新镜像ID /work/monitor
```

不要修改已经部署节点的 `NPU_AGENT_NODE_ID`，否则 Collector 会将其识别为一台新机器。删除容器不会删除宿主机 `/work/monitor/runtime-data/agent/` 中的数据。

### 6.4 Collector 数据备份

不要在 Collector 运行时只复制 `cluster.sqlite3`，因为 WAL 文件中可能还有已提交数据，而且长期历史位于 `archive/cluster-archive.sqlite3`。简单可靠的离线备份方式是停止 Collector 后复制整个目录：

```bash
docker stop -t 15 npu-monitor-collector
cp -a /work/monitor/runtime-data/collector /备份目录/collector-$(date +%F-%H%M%S)
docker start npu-monitor-collector
```

## 7. 常见问题排查

### 7.1 Agent 无法上传

```bash
curl -v http://主节点IP:18080/health
cat /work/monitor/runtime-data/agent/upload_health.json
find /work/monitor/runtime-data/agent/spool -name '*.json' | wc -l
```

网络恢复后 Agent 会自动补传。告警建议使用：

```text
pending_samples > 0 AND oldest_pending_age_seconds > alert_threshold
```

上传失败次数和失败起始时间会跨 Agent 重启保存，不会因为重启而清零。

### 7.2 Agent 没有识别到 8 张卡

```bash
docker exec npu-monitor-agent npu-smi info
docker logs --tail 200 npu-monitor-agent
cat /work/monitor/runtime-data/agent/health.json
```

创建脚本会在运行前检查 `/dev/davinci0` 至 `/dev/davinci7`、管理设备和驱动挂载源。任何路径缺失都会报错，不会静默创建一个无法采集的容器。

### 7.3 检查僵尸进程

```bash
docker exec npu-monitor-agent ps -o pid,ppid,stat,args -p 1
docker exec npu-monitor-agent ps -eo pid,ppid,stat,args | awk '$3 ~ /^Z/'
```

PID 1 应显示 `/work/monitor/deploy/mini_init.py`，第二条命令不应输出任何 `Z` 状态进程。不要在容器中用 `nohup python3 ... &` 启动 Agent。

### 7.4 Console 显示 stale 或 offline

- `stale`：超过约两个采集周期没有新样本；
- `offline`：超过约五个采集周期没有新样本；
- 先检查 Agent 是否仍在运行，再检查 `upload_health.json` 和 Collector 网络；
- stale/offline 节点仍保留登记容量，但过期卡数据不参与当前利用率。

## 8. 常用查询

Console Web：

```text
http://主节点IP:18081/
```

Web 页面支持集群筛选、自定义开始/结束时间、5 分钟到 1 天的聚合粒度、指定节点和卡号、节点搜索与状态筛选、采样状态与节点搜索、告警类型/级别/状态、三张明细表独立分页，以及 CSV/XLSX 下载。页面参数与下方 HTTP 查询接口一致。

命令行查询集群概览、分组和节点：

```bash
cd /work/monitor
export PYTHONPATH=/work/monitor

python3 -m console.cli \
  --collector-url http://主节点IP:18080 summary

python3 -m console.cli \
  --collector-url http://主节点IP:18080 nodes --state offline

python3 -m console.cli \
  --collector-url http://主节点IP:18080 clusters

python3 -m console.cli \
  --collector-url http://主节点IP:18080 nodes \
  --cluster training-a --search npu-node --page 1 --page-size 50
```

查询集群、节点或单卡历史：

```bash
python3 -m console.cli \
  --collector-url http://主节点IP:18080 history --hours 24 --bucket 300

python3 -m console.cli \
  --collector-url http://主节点IP:18080 history \
  --start 2026-08-01 --end 2026-08-07 --bucket 3600 \
  --cluster training-a --node npu-node-01 --card 0
```

查询原始采样和告警：

```bash
python3 -m console.cli \
  --collector-url http://主节点IP:18080 samples \
  --hours 24 --cluster training-a --status partial

python3 -m console.cli \
  --collector-url http://主节点IP:18080 alerts \
  --hours 168 --cluster training-a --status active
```

导出指定日期范围。日期形式的 `--end` 按包含当天处理；单次导出默认最多 31 天：

```bash
python3 -m console.cli \
  --collector-url http://主节点IP:18080 export \
  --start 2026-08-01 --end 2026-08-07 \
  --cluster training-a --format xlsx \
  --output /work/monitor/runtime-data/npu-2026-08-01_to_07.xlsx

python3 -m console.cli \
  --collector-url http://主节点IP:18080 export \
  --start 2026-08-01 --end 2026-08-01 \
  --format csv --output /work/monitor/runtime-data/npu-2026-08-01.csv
```

XLSX 和 CSV 都是一行一张卡。字段包含集群、节点、采样/接收时间、采样状态、覆盖率、卡号、利用率和 HBM。XLSX 仍由 Python 标准库生成，不需要额外安装包。

生成按节点聚合的利用率与热力图 XLSX：

```bash
# 当前报表时区当天 00:00 到当前时间
python3 -m console.cli --collector-url http://主节点IP:18080 \
  utilization-report --period day \
  --output /work/monitor/runtime-data/npu-utilization-day.xlsx

# 当前报表时区本月 1 日 00:00 到当前时间
python3 -m console.cli --collector-url http://主节点IP:18080 \
  utilization-report --period month \
  --output /work/monitor/runtime-data/npu-utilization-month.xlsx

# 自定义时间，可增加 --cluster、--node 或 --timezone UTC
python3 -m console.cli --collector-url http://主节点IP:18080 \
  utilization-report --period custom \
  --start 2026-08-01 --end 2026-08-07 \
  --cluster training-a --timezone UTC+08:00 \
  --output /work/monitor/runtime-data/npu-utilization-custom.xlsx
```

首个 `汇总` Sheet 按节点给出查询区间平均利用率，并以每天的节点平均利用率组成热力图；后续每个报表时区日期一个 Sheet，展示节点当天平均利用率及 0～23 时的小时热力图。平均值直接使用该节点在对应范围内的全部逐卡数据点计算。

Console HTTP 接口：

全部查询条件、CLI 命令、HTTP 参数、统计口径及返回边界请参见 [QUERY_GUIDE.md](QUERY_GUIDE.md)。

| 路径 | 作用 |
|---|---|
| `/health` | Console 和上游 Collector 健康状态 |
| `/api/snapshot?cluster_id=training-a` | 当前集群快照 |
| `/api/clusters` | 集群分组汇总 |
| `/api/nodes?page=1&page_size=50&q=node&state=offline` | 节点搜索、状态筛选和分页 |
| `/api/history?start=...&end=...&bucket=300&node_id=...&card_id=0` | 集群、节点或单卡历史 |
| `/api/samples?hours=24&page=1&page_size=100` | 原始采样分页 |
| `/api/alerts?hours=168&status=active` | 告警记录与生命周期 |
| `/api/export?start=...&end=...&format=xlsx` | 下载 CSV/XLSX 报表 |
| `/api/utilization-report?period=month` | 下载按节点聚合的局点时区平均利用率和热力图 XLSX |
| `POST /api/admin/preview` | 预览节点修改、节点删除或错误样本删除的影响范围 |
| `POST /api/admin/execute` | 携带预览返回的确认令牌执行操作并自动备份 |
| `/api/admin/operations?page=1&page_size=50` | 查询管理操作审计记录 |

### 8.1 错误节点和错误数据管理

在 Console 页面底部使用“错误节点与错误数据管理”。所有操作都必须先预览；预览会返回热库与归档库中受影响的节点、采样、逐卡记录和告警数量。确认执行时 Collector 会重新计算影响范围，只有结果未变化且确认令牌一致才执行。如果预览后又收到相关样本，执行会返回冲突，必须重新预览。

支持三种操作：

- 修改已登记节点的展示名称或集群；
- 删除错误节点及其全部热数据，可选择同时处理归档库；
- 删除一条采样，或按顶部时间范围加节点/集群/采样状态删除错误数据，可选择同时删除重叠告警和归档数据。

不支持直接修改 `node_id`。正确处理错误 `node_id` 的顺序是：

1. 先停止对应 Agent，修正 `NPU_AGENT_NODE_ID`、节点名称或集群配置；
2. 启动 Agent 并确认新节点信息正确；
3. 在 Console 选择旧节点，预览影响范围；
4. 确认自动备份路径后删除旧节点。

如果没有先修正 Agent，删除的错误节点会在下一次上报时重新出现；节点名称或集群的页面修正也可能被 Agent 后续上报覆盖。

破坏性操作默认同时处理归档库。按时间范围删除时必须至少填写精确 `node_id` 或 `cluster_id`，后端拒绝没有节点/集群限制的全库删除。每次成功操作都会写入 `admin_operations` 审计表，备份文件位于：

```text
/work/monitor/runtime-data/collector/backups/
├── cluster-before-delete_node-时间-操作ID.sqlite3
└── cluster-archive-before-delete_node-时间-操作ID.sqlite3
```

备份不会自动删除。确认数据无误并完成离线备份后，再由管理员人工处理旧备份。

无浏览器环境也可以使用两阶段命令。第一次命令只预览并返回 `confirmation_token`，把令牌原样放到第二次完全相同的命令中才会执行：

```bash
python3 -m console.cli --collector-url http://主节点IP:18080 \
  node-delete --node wrong-node-01

python3 -m console.cli --collector-url http://主节点IP:18080 \
  node-delete --node wrong-node-01 --confirm 预览返回的完整令牌

python3 -m console.cli --collector-url http://主节点IP:18080 \
  data-delete --start 2026-08-01 --end 2026-08-02 \
  --node npu-node-01 --status failed

python3 -m console.cli --collector-url http://主节点IP:18080 operations
```

## 9. 配置参考

### 9.1 Agent

| 环境变量 | 默认值 | 说明 |
|---|---:|---|
| `NPU_AGENT_NODE_ID` | 必填 | 稳定且唯一的节点编号 |
| `NPU_AGENT_NODE_NAME` | 主机名 | 展示名称 |
| `NPU_AGENT_CLUSTER_ID` | `default` | 节点所属集群或业务分组 |
| `NPU_AGENT_COLLECTOR_URL` | 必填 | Collector 地址 |
| `NPU_AGENT_EXPECTED_CARDS` | 8 | 预期逻辑NPU数下限；910C会按Phy-ID自动扩展到16，正式部署建议显式设置16 |
| `NPU_AGENT_COLLECT_INTERVAL` | 60 | 采集周期，秒 |
| `NPU_AGENT_COMMAND_TIMEOUT` | 10 | `npu-smi` 超时 |
| `NPU_AGENT_HTTP_TIMEOUT` | 5 | 单次 HTTP 超时 |
| `NPU_AGENT_RETENTION_DAYS` | 180 | 本地热数据窗口；超期文件转入归档，不删除 |
| `NPU_AGENT_ARCHIVE_DIR` | 数据目录下的 `archive` | Agent 永久归档目录；容器部署时应位于 `/work/monitor` 挂载内 |
| `NPU_AGENT_SPOOL_RETENTION_DAYS` | 7 | 未上传队列最大保留天数 |
| `NPU_AGENT_SPOOL_MAX_FILES` | 20000 | 队列文件数上限 |
| `NPU_AGENT_SPOOL_MAX_BYTES` | 536870912 | 队列字节上限 |
| `NPU_AGENT_UPLOAD_BATCH_SIZE` | 100 | 每轮最多补传条数 |
| `NPU_AGENT_MIN_FREE_BYTES` | 104857600 | 本地最小剩余空间 |
| `NPU_AGENT_MIN_FREE_INODES` | 1000 | 本地最小剩余 inode |
| `NPU_AGENT_NPU_SMI_BIN` | `npu-smi` | 测试时可指定其他命令 |
| `NPU_AGENT_MONTHLY_XLSX_ENABLED` | `true` | 是否每天从 CSV 更新月度 XLSX |

### 9.2 Collector

| 环境变量 | 默认值 | 说明 |
|---|---:|---|
| `NPU_COLLECTOR_PORT` | 18080 | 监听端口 |
| `NPU_COLLECTOR_RETENTION_DAYS` | 180 | 在线查询热数据窗口；超期数据转入归档库 |
| `NPU_COLLECTOR_ARCHIVE_DIR` | 数据目录下的 `archive` | Collector 永久归档目录；容器部署时应位于 `/work/monitor` 挂载内 |
| `NPU_COLLECTOR_BACKUP_DIR` | 数据目录下的 `backups` | 数据管理操作执行前的自动 SQLite 备份目录 |
| `NPU_COLLECTOR_STALE_SECONDS` | 120 | stale 最小阈值 |
| `NPU_COLLECTOR_OFFLINE_SECONDS` | 300 | offline 最小阈值 |
| `NPU_COLLECTOR_MAX_FUTURE_SECONDS` | 300 | 允许的最大未来时间偏差 |
| `NPU_COLLECTOR_CLOCK_SKEW_WARN_SECONDS` | 30 | 相邻采样时钟偏移变化的警告阈值 |
| `NPU_COLLECTOR_BUSY_UTILIZATION` | 80 | 高负载卡阈值 |
| `NPU_COLLECTOR_IDLE_UTILIZATION` | 10 | 空闲卡阈值 |
| `NPU_COLLECTOR_MIN_FREE_BYTES` | 209715200 | 最小剩余空间 |
| `NPU_COLLECTOR_MIN_FREE_INODES` | 1000 | 最小剩余 inode |

### 9.3 Console

| 环境变量 | 默认值 | 说明 |
|---|---:|---|
| `NPU_CONSOLE_COLLECTOR_URL` | `http://127.0.0.1:18080` | Collector 地址 |
| `NPU_CONSOLE_PORT` | 18081 | Web 端口 |
| `NPU_CONSOLE_HTTP_TIMEOUT` | 10 | 访问 Collector 的超时 |
| `NPU_CONSOLE_MAX_EXPORT_DAYS` | 31 | 单次 CSV/XLSX 最大导出天数 |
| `NPU_CONSOLE_REPORT_TIMEOUT` | 120 | 汇聚报表查询 Collector 的超时秒数 |
| `NPU_CONSOLE_MAX_REPORT_DAYS` | 180 | 自定义汇聚报表最大时间范围天数 |

### 9.4 容器公共配置

| 环境变量 | 默认值 | 说明 |
|---|---:|---|
| `NPU_MONITOR_RESTART_POLICY` | `unless-stopped` | Docker 重启策略 |
| `NPU_MONITOR_STOP_TIMEOUT` | 30 | 停止超时，秒 |
| `NPU_MONITOR_INIT_KILL_AFTER` | 20 | mini-init 强制结束子进程前的等待时间，秒 |
| `NPU_MONITOR_LOG_MAX_SIZE` | `20m` | 单个 Docker 日志文件上限 |
| `NPU_MONITOR_LOG_MAX_FILES` | 5 | Docker 日志文件数量 |
| `NPU_MONITOR_SHM_SIZE` | `500g` | Agent 容器共享内存大小 |
| `NPU_MONITOR_TIMEZONE` | `auto` | 本地 CSV、月度 XLSX 和汇聚热力图使用的时区；支持 `auto`、`UTC`、`UTC+08:00` 等固定偏移 |

`auto` 会读取容器的本地 UTC 偏移，三个创建脚本会自动挂载宿主机时区文件。可在部署前按局点覆盖：

```bash
# 宿主机/容器时区可信时，无需设置
unset NPU_MONITOR_TIMEZONE

# 机器显示 UTC，但报表需要东八区
export NPU_MONITOR_TIMEZONE=UTC+08:00

# 局点明确要求使用 UTC
export NPU_MONITOR_TIMEZONE=UTC
```

同一局点建议用同一个值创建 Agent、Collector 和 Console。也可以在单次 CLI 报表增加 `--timezone UTC`，或在 `/api/utilization-report` 增加 `timezone=UTC` 临时覆盖。`/health` 和 Agent 的 `health.json` 会显示实际采用的规范化时区，例如 `UTC+00:00` 或 `UTC+08:00`。

## 10. 数据口径

- `online`：Collector最近收到该节点的数据且最新采样卡数完整；
- `degraded`：节点仍在上报，但最新采样缺卡或采集失败；
- `stale`：Collector超过约两个采集周期没有收到该节点的新数据；
- `offline`：Collector超过约五个采集周期没有收到该节点的新数据；
- 当前集群利用率按所有新鲜有效卡加权，不使用“节点平均值再平均”；
- 登记容量包含所有已登记节点的预期卡数，不因节点 stale/offline 而消失；
- 舰队新鲜度等于新鲜卡样本数除以全部登记容量；
- 历史覆盖率等于实际卡样本数除以预期卡样本数；
- HBM 百分比按所有具有完整 HBM 数据的卡汇总计算；
- stale/offline 节点的过期数据不参与当前利用率；
- 节点状态按Collector的接收时间判断，不要求所有Agent与主节点的绝对时间完全一致；
- 固定的Agent时间偏差只记录为 `clock_offset_seconds`，不会触发告警；相邻样本的偏移量变化超过阈值时才产生时钟告警；
- 协议与 Collector SQLite 中的原始采样时间仍统一转换为 UTC 保存，因此断线补传不会被错误地记到补传时刻；Agent 本地 CSV、状态文件、月度 XLSX 和汇聚报表按 `NPU_MONITOR_TIMEZONE` 的实际偏移展示和分组；
- 180 天是默认热查询窗口，不是数据寿命；超期采样、逐卡明细和已恢复告警经写入校验后进入独立归档库，程序不自动删除归档；
- 未设置 `NPU_AGENT_CLUSTER_ID` 的旧节点自动归入 `default`；`NPU_AGENT_NODE_ID` 仍需在整个 Collector 中全局唯一；
- 告警会记录 `degraded`、`stale`、`offline`、卡覆盖率不足和时钟偏差，并保留 active/resolved 生命周期；
- Collector 首次启动新版本时会把旧 SQLite schema 1/2 原地升级为 schema 3，不需要删除数据库；schema 3 增加管理操作审计表。

## 11. 数据链路和组件边界

每轮采集的数据路径是：

```text
npu-smi -> Agent 本地 CSV/状态 -> 每日更新月度 XLSX
        -> Agent spool -> HTTP POST
        -> Collector 校验/去重/热 SQLite/告警生命周期 -> 冷归档 SQLite
        -> Console 查询、筛选、展示和 CSV/XLSX 导出
```

Agent 负责采集和可靠上传；Collector 只负责接收、校验、存储和生成基础汇聚数据；Console 负责命令行及 Web 展示。网络中断不会停止本地采集，恢复后会按原始采样时间补传；重复样本不会重复计数，内容冲突的同身份样本会被拒绝并留在 Agent 的 `rejected` 目录中。

Collector 的热库为 `/work/monitor/runtime-data/collector/cluster.sqlite3`，冷归档库为 `/work/monitor/runtime-data/collector/archive/cluster-archive.sqlite3`。归档采用“先写入并校验、再从热库移除”的顺序；任一步失败都会保留热库原数据。当前 Console 和 HTTP 查询只访问热库，归档库用于长期留存、备份和离线审计，不会混入实时统计。归档数据默认无限期增长，请监控宿主机容量并把整个 `runtime-data` 目录纳入备份。

项目根目录的 `npu_monitor.py`、`start.sh` 和 `stop.sh` 属于兼容保留的旧单机模式。新集群部署使用 `agent/`、`collector/`、`console/` 和 `cluster_common/`。
