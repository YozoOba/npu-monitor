# NPU 集群监控测试与验收指南

## 1. 建议顺序

先在普通开发机完成标准库测试，再在一台主节点和一台 910B 节点完成灰度，最后扩展到十几台机器。不要直接同时替换所有节点。

验收分为：

1. 代码测试；
2. Collector 接收、校验和去重；
3. Console 查询与页面；
4. Agent 真实 910B 解析；
5. 断网补传；
6. 容器启停和僵尸检查；
7. 多节点数据准确性；
8. 24 小时稳定性。

## 2. 本地自动测试

项目根目录执行：

```bash
python3 -m unittest discover -s tests -p 'test_cluster.py' -v
python3 test_tool.py
```

预期：

```text
Ran 14 tests
OK

Ran 14 tests
OK
```

集群测试覆盖协议标准化、完整/部分采样、非法卡号、未来时间、SQLite 事务、重复补传、冲突拒绝、历史回填、在线状态、加权平均、覆盖率、HTTP 接收、磁盘队列状态恢复和独立 Console。旧的 14 项单机测试用于保证原有解析和单机功能没有回归。

## 3. 复用已有镜像并创建测试容器

```bash
cd /work/monitor
chmod +x deploy/create_npu_monitor_container.sh
docker image inspect 13315b656180 >/dev/null
./deploy/create_npu_monitor_container.sh 13315b656180 /work/monitor
```

检查复用的 `vllm-ascend` 镜像是否具备运行条件：

```bash
docker inspect -f '{{.HostConfig.Init}}' npu-monitor
docker exec npu-monitor python3 --version
docker exec npu-monitor npu-smi info
```

预期 `HostConfig.Init` 为 `true`，并能看到 Python 版本和 8 张 NPU。此流程不构建任何新镜像。

## 4. Collector 单节点测试

在主节点项目目录启动测试 Collector：

```bash
cd /work/monitor
export PYTHONPATH=/work/monitor
export NPU_COLLECTOR_DATA_DIR=/work/monitor/runtime-data/collector-test
export NPU_COLLECTOR_PORT=18080
mkdir -p "$NPU_COLLECTOR_DATA_DIR"
python3 -u -m collector.app
```

保持该进程在前台运行，在另一个终端健康检查：

```bash
curl -sS http://127.0.0.1:18080/health
```

预期 `status` 为 `healthy`，数据库完整性为 `ok`。

## 5. 手工发送有效样本

```bash
NOW=$(date -u +%Y-%m-%dT%H:%M:%S+00:00)
curl -i -X POST http://127.0.0.1:18080/api/v1/samples \
  -H 'Content-Type: application/json' \
  --data-binary "{
    \"protocol_version\": 1,
    \"node_id\": \"test-node-01\",
    \"node_name\": \"test-910b-01\",
    \"collected_at\": \"$NOW\",
    \"collect_interval\": 60,
    \"expected_cards\": 2,
    \"cards\": [
      {\"card_id\":0,\"utilization\":20,\"hbm_used_mb\":100,\"hbm_total_mb\":1000},
      {\"card_id\":1,\"utilization\":80,\"hbm_used_mb\":500,\"hbm_total_mb\":1000}
    ]
  }"
```

第一次预期 HTTP 201。立即原样再发送一次，预期 HTTP 200 且：

```json
{"accepted": true, "duplicate": true}
```

确认数据库仍只有一份采样：

```bash
cd /work/monitor
NPU_COLLECTOR_DATA_DIR=/work/monitor/runtime-data/collector-test \
  python3 -m collector.app --check-db
```

## 6. 数据准确性拒收测试

把上一请求中的利用率改成 `101` 再发送，预期 HTTP 400。以下情况也必须拒收且不能进入数据库：

- 同一次采样出现重复 `card_id`；
- `card_id` 超出 `0 .. expected_cards-1`；
- 利用率小于 0 或大于 100；
- HBM 使用量大于容量；
- 时间没有时区；
- 时间比 Collector 快超过允许阈值；
- 卡字段类型不正确。

相同 `node_id + collected_at` 使用不同利用率再次发送，预期 HTTP 409。这代表同一采样身份发生数据冲突，不能用后来的值静默覆盖原值。

## 7. Console 测试

```bash
cd /work/monitor
export PYTHONPATH=/work/monitor
export NPU_CONSOLE_COLLECTOR_URL=http://主节点IP:18080
export NPU_CONSOLE_PORT=18081
python3 -u -m console.web
```

检查：

```bash
curl -sS http://127.0.0.1:18081/health
curl -sS http://127.0.0.1:18081/api/snapshot
curl -sS 'http://127.0.0.1:18081/api/history?hours=24&bucket=300'
```

浏览器打开 `http://Console节点IP:18081/`，应看到节点数、登记容量、新鲜样本、舰队新鲜度、上报完整度、当前利用率、HBM、24 小时趋势和节点表格。

命令行交叉验证：

```bash
python3 -m console.cli --collector-url http://主节点IP:18080 summary
python3 -m console.cli --collector-url http://主节点IP:18080 nodes
python3 -m console.cli --collector-url http://主节点IP:18080 history --hours 24
```

## 8. Agent 真实解析测试

先在 910B Agent 镜像中直接执行：

```bash
npu-smi info
python3 - <<'PY'
from agent.sampler import collect
cards, error = collect('npu-smi', 10)
print('error:', error)
for card in cards:
    print(card)
raise SystemExit(0 if len(cards) == 8 else 1)
PY
```

验收条件：

- 正好识别 8 张卡；
- 卡号是 0–7 且不重复；
- 利用率与人工查看的 `npu-smi info` 一致；
- 如果输出能提供 HBM，使用量和总量与原始输出一致；
- 解析脚本不产生额外常驻 Python 进程。

## 9. Agent 单次端到端测试

在第 3 节创建的测试容器中执行单次采集：

```bash
mkdir -p /work/monitor/runtime-data/agent-test
docker exec \
  -e NPU_AGENT_NODE_ID=test-910b-01 \
  -e NPU_AGENT_COLLECTOR_URL=http://主节点IP:18080 \
  -e NPU_AGENT_EXPECTED_CARDS=8 \
  -e NPU_AGENT_DATA_DIR=/work/monitor/runtime-data/agent-test \
  -e PYTHONPATH=/work/monitor \
  npu-monitor bash -lc 'cd /work/monitor && python3 -u -m agent.app --once'
```

检查本地：

```bash
find /work/monitor/runtime-data/agent-test -maxdepth 2 -type f -print
cat /work/monitor/runtime-data/agent-test/health.json
cat /work/monitor/runtime-data/agent-test/upload_health.json
tail -10 /work/monitor/runtime-data/agent-test/daily/stats_$(date -u +%F).csv
```

检查 Console 中出现 `test-910b-01`，卡数为 `8/8`，状态为 `online`。

## 10. 断网排队和恢复补传

正式启动一个灰度 Agent 后，在 Collector 前台终端按 `Ctrl+C` 停止 Collector。

等待至少三个采集周期，检查：

```bash
find /work/monitor/runtime-data/agent/spool -name '*.json' | wc -l
cat /work/monitor/runtime-data/agent/upload_health.json
tail -5 /work/monitor/runtime-data/agent/sample_status/samples_$(date -u +%F).jsonl
```

验收条件：

- Agent 容器没有退出；
- 本地 CSV 仍持续追加；
- `spool` 数量随采样增长；
- 上传错误被记录，但不影响下一轮采集。

按照第 4 节命令重新启动 Collector：

```bash
cd /work/monitor
export PYTHONPATH=/work/monitor
export NPU_COLLECTOR_DATA_DIR=/work/monitor/runtime-data/collector-test
export NPU_COLLECTOR_PORT=18080
python3 -u -m collector.app
```

等待补传，检查 `spool` 最终归零，Collector 历史中包含断网期间的时间桶，且没有重复记录。

在 Collector 仍不可用时重启 Agent，检查 `upload_health.json`：

- `last_success` 不会变回 `null`；
- `consecutive_failures` 不会因为重启清零；
- `upload_unavailable_since` 保持原值；
- `oldest_pending_at` 和 `oldest_pending_age_seconds` 继续增长；
- 成功补传并清空队列后，`upload_unavailable_since` 才恢复为 `null`。

## 11. 部分采样和失败采样

不要主动断开生产 NPU。可以在测试容器中将预期卡数暂时设为 9：

```bash
-e NPU_AGENT_EXPECTED_CARDS=9
```

实际采到 8 张卡时应满足：

- Agent `sample_status=partial`；
- 缺失卡号包含 8；
- 覆盖率为 88.89%；
- Collector 节点状态为 `degraded`；
- 已采到的 8 张卡仍参与集群统计；
- 历史覆盖率分母按 9 张卡计算。

使用不存在的命令可以测试完全失败：

```bash
-e NPU_AGENT_NPU_SMI_BIN=/not-exist/npu-smi
```

预期上报一条 `failed` 采样而不是假装节点离线，Collector 能区分“节点在线但采集失败”和“节点没有上报”。

## 12. stale/offline 状态测试

灰度环境可临时使用较短阈值：

```bash
-e NPU_COLLECTOR_STALE_SECONDS=10 \
-e NPU_COLLECTOR_OFFLINE_SECONDS=25
```

停止 Agent 后，状态应先从 `online/degraded` 变为 `stale`，再变为 `offline`。实际生效阈值还会取 Agent 采集周期的 2 倍和 5 倍中的较大值；若 Agent 周期为 60 秒，需要分别等待约 120 秒和 300 秒。

## 13. 重启和持久化测试

记录重启前健康信息，在 Collector 前台终端按 `Ctrl+C`，再使用相同数据目录重新执行第 4 节启动命令：

```bash
curl -sS http://主节点IP:18080/health
NPU_COLLECTOR_DATA_DIR=/work/monitor/runtime-data/collector-test \
  PYTHONPATH=/work/monitor python3 -u -m collector.app
curl -sS http://主节点IP:18080/health
```

验收条件：

- `sample_count` 没有归零；
- 已登记节点仍在快照中；
- Agent 在 Collector 重启时产生的队列会自动补传；
- SQLite `database_integrity` 仍为 `ok`。

再重启 Agent，确认旧 `spool` 会先被继续发送，不会因为进程重启丢失。

## 14. 僵尸进程回归测试

确认容器使用 `--init`：

```bash
docker inspect -f '{{.HostConfig.Init}}' npu-monitor
docker exec npu-monitor ps -o pid,ppid,stat,args -p 1
```

连续启停 20 次：

```bash
for i in $(seq 1 20); do
  docker stop -t 15 npu-monitor || exit 1
  docker start npu-monitor || exit 1
  sleep 2
done
```

检查：

```bash
docker exec npu-monitor sh -c \
  "ps -eo pid,ppid,stat,args | awk '\$3 ~ /^Z/ {print}'"
```

验收条件是没有 `Z` 状态进程。集群 Agent 的 HTTP 上报在线程中运行，不会为发送请求创建新的 Python 进程；`npu-smi` 子进程由 Python `subprocess.run()` 同步等待回收。

## 15. 多节点准确性抽查

先灰度 2 台，每台 8 卡。Console 预期显示：

- 总节点数 2；
- 预期卡数 16；
- 完整采集时活动卡数 16、覆盖率 100%；
- 集群利用率等于 16 张卡当前值的算术平均，而不是两个节点平均值的简单平均；
- HBM 等于所有具有 HBM 数据的卡使用量总和除以容量总和。

人工计算一轮：

```text
集群利用率 = 所有有效卡 utilization 之和 / 有效卡数量
覆盖率 = 实际收到的卡样本数量 / 预期卡样本数量
```

抽查同一 UTC 时间桶的 Agent CSV、Collector 序列输出和 Console，三者数值应一致。

## 16. 24 小时稳定性验收

灰度运行至少 24 小时后检查：

- Agent 和 Collector 容器没有异常重启；
- Agent `spool` 通常为 0；
- `rejected` 没有新文件；
- 每个完整采样都包含 8 卡；
- 集群历史覆盖率符合实际网络和设备状态；
- SQLite 完整性为 `ok`；
- Collector 数据目录和 Agent 数据目录增长符合预期；
- 无僵尸进程；
- 停止任一 Agent 后能正确进入 stale/offline，恢复后自动 online；
- Collector 重启期间数据可补传且不重复。

通过后再逐批扩展到其余机器，建议每批 3–5 台。
