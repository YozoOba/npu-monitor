# NPU 集群监控测试与验收指南

## 1. 建议顺序

先在普通开发机完成标准库测试，再在一台主节点和一台 910B 节点完成灰度，最后扩展到十几台机器。不要直接同时替换所有节点。

验收分为：

1. 代码测试；
2. Collector 接收、校验和去重；
3. Console 查询与页面；
4. Agent 真实 910B 解析；
5. 断网补传；
6. 容器启停和僵尸检查；
7. 多节点数据准确性；
8. 24 小时稳定性。

## 2. 本地自动测试

项目根目录执行：

```bash
python3 -m unittest discover -s tests -p 'test_*.py' -v
python3 test_tool.py
```

预期：

```text
Ran 30 tests
OK

Ran 14 tests
OK
```

30 项集群测试覆盖协议标准化、完整/部分采样、非法卡号、未来时间、SQLite 事务与 schema 1 原地升级、重复补传、冲突拒绝、历史回填、在线状态、加权平均、覆盖率、多集群和单卡筛选、节点 ID 跨集群唯一性、原始样本分页、告警生命周期、HTTP 接收、CSV/XLSX 导出、磁盘队列状态恢复、独立 Console、Agent 月度 XLSX，以及 mini-init 的退出码、信号转发和孤儿进程回收。旧的 14 项单机测试用于保证原有解析和单机功能没有回归。

## 3. 复用已有镜像和脚本检查

```bash
cd /work/monitor
chmod +x deploy/*.sh
docker image inspect 13315b656180 >/dev/null
bash -n deploy/create_agent_container.sh
bash -n deploy/create_collector_container.sh
bash -n deploy/create_console_container.sh
python3 -m py_compile deploy/mini_init.py
```

检查复用的 `vllm-ascend` 镜像是否具备运行条件：

```bash
docker run --rm --entrypoint python3 13315b656180 --version
```

此流程不构建任何新镜像，也不依赖 Docker `--init`。

## 4. Collector 单节点测试

在主节点项目目录启动测试 Collector：

```bash
cd /work/monitor
export NPU_COLLECTOR_PORT=18080
./deploy/create_collector_container.sh 13315b656180 /work/monitor
```

健康检查：

```bash
curl -sS http://127.0.0.1:18080/health
```

预期 `status` 为 `healthy`，数据库完整性为 `ok`。

## 5. 手工发送有效样本

```bash
NOW=$(date -u +%Y-%m-%dT%H:%M:%S+00:00)
curl -i -X POST http://127.0.0.1:18080/api/v1/samples \
  -H 'Content-Type: application/json' \
  --data-binary "{
    \"protocol_version\": 1,
    \"node_id\": \"test-node-01\",
    \"node_name\": \"test-910b-01\",
    \"collected_at\": \"$NOW\",
    \"collect_interval\": 60,
    \"expected_cards\": 2,
    \"cards\": [
      {\"card_id\":0,\"utilization\":20,\"hbm_used_mb\":100,\"hbm_total_mb\":1000},
      {\"card_id\":1,\"utilization\":80,\"hbm_used_mb\":500,\"hbm_total_mb\":1000}
    ]
  }"
```

第一次预期 HTTP 201。立即原样再发送一次，预期 HTTP 200 且：

```json
{"accepted": true, "duplicate": true}
```

确认数据库仍只有一份采样：

```bash
cd /work/monitor
docker exec npu-monitor-collector \
  python3 -m collector.app --check-db
```

## 6. 数据准确性拒收测试

把上一请求中的利用率改成 `101` 再发送，预期 HTTP 400。以下情况也必须拒收且不能进入数据库：

- 同一次采样出现重复 `card_id`；
- `card_id` 超出 `0 .. expected_cards-1`；
- 利用率小于 0 或大于 100；
- HBM 使用量大于容量；
- 时间没有时区；
- 时间比 Collector 快超过允许阈值；
- 卡字段类型不正确。

相同 `node_id + collected_at` 使用不同利用率再次发送，预期 HTTP 409。这代表同一采样身份发生数据冲突，不能用后来的值静默覆盖原值。

## 7. Console 测试

```bash
export NPU_CONSOLE_COLLECTOR_URL=http://主节点IP:18080
export NPU_CONSOLE_PORT=18081
./deploy/create_console_container.sh 13315b656180 /work/monitor
```

检查：

```bash
curl -sS http://127.0.0.1:18081/health
curl -sS http://127.0.0.1:18081/api/snapshot
curl -sS 'http://127.0.0.1:18081/api/history?hours=24&bucket=300'
curl -sS 'http://127.0.0.1:18081/api/nodes?page=1&page_size=50&q=test'
curl -sS 'http://127.0.0.1:18081/api/samples?hours=24&page=1&page_size=10'
curl -sS 'http://127.0.0.1:18081/api/alerts?hours=24&status=active'
```

浏览器打开 `http://Console节点IP:18081/`，应看到节点数、登记容量、新鲜样本、舰队新鲜度、上报完整度、当前利用率、HBM、可调时间范围趋势、集群分组、可搜索分页节点表、告警表和原始采样表。

命令行交叉验证：

```bash
python3 -m console.cli --collector-url http://主节点IP:18080 summary
python3 -m console.cli --collector-url http://主节点IP:18080 clusters
python3 -m console.cli --collector-url http://主节点IP:18080 nodes
python3 -m console.cli --collector-url http://主节点IP:18080 history --hours 24
python3 -m console.cli --collector-url http://主节点IP:18080 history --hours 24 --node test-910b-01 --card 0
python3 -m console.cli --collector-url http://主节点IP:18080 samples --hours 24 --page-size 10
python3 -m console.cli --collector-url http://主节点IP:18080 alerts --hours 24
```

测试报表导出：

```bash
python3 -m console.cli \
  --collector-url http://主节点IP:18080 export \
  --start 2026-08-01 --end 2026-08-01 \
  --format csv --output /tmp/npu-export.csv

python3 -m console.cli \
  --collector-url http://主节点IP:18080 export \
  --start 2026-08-01 --end 2026-08-01 \
  --format xlsx --output /tmp/npu-export.xlsx

python3 -m zipfile -l /tmp/npu-export.xlsx
```

验收 CSV 和 XLSX 的数据行数相同，每行对应一张卡；抽查 `cluster_id`、`node_id`、`collected_at`、`card_id`、`utilization` 和 HBM 与原始采样一致。XLSX 应具有冻结表头、筛选器、可读列宽和数值类型的利用率/HBM 单元格。

## 8. Agent 真实解析测试

先在 910B Agent 镜像中直接执行：

```bash
npu-smi info
python3 - <<'PY'
from agent.sampler import collect
cards, error = collect('npu-smi', 10)
print('error:', error)
for card in cards:
    print(card)
raise SystemExit(0 if len(cards) == 8 else 1)
PY
```

验收条件：

- 正好识别 8 张卡；
- 卡号是 0–7 且不重复；
- 利用率与人工查看的 `npu-smi info` 一致；
- 如果输出能提供 HBM，使用量和总量与原始输出一致；
- 解析脚本不产生额外常驻 Python 进程。

## 9. Agent 端到端测试

启动一个真实 Agent 容器：

```bash
export NPU_AGENT_NODE_ID=test-910b-01
export NPU_AGENT_NODE_NAME=test-910b-01
export NPU_AGENT_CLUSTER_ID=test-cluster
export NPU_AGENT_COLLECTOR_URL=http://主节点IP:18080
export NPU_AGENT_EXPECTED_CARDS=8
./deploy/create_agent_container.sh 13315b656180 /work/monitor
```

检查本地：

```bash
find /work/monitor/runtime-data/agent -maxdepth 2 -type f -print
cat /work/monitor/runtime-data/agent/health.json
cat /work/monitor/runtime-data/agent/upload_health.json
tail -10 /work/monitor/runtime-data/agent/daily/stats_$(date -u +%F).csv
```

检查 Console 中出现 `test-910b-01`，卡数为 `8/8`，状态为 `online`。

月度 XLSX 默认在跨入下一个 UTC 日期后的第一次采集时更新。存在前一日 CSV 时，重启 Agent 后检查：

```bash
ls -lh /work/monitor/runtime-data/agent/monthly/
MONTH=$(python3 -c 'from datetime import datetime,timedelta,timezone; print((datetime.now(timezone.utc)-timedelta(days=1)).strftime("%Y-%m"))')
python3 -m zipfile -l \
  /work/monitor/runtime-data/agent/monthly/stats_${MONTH}.xlsx
```

用 Excel 或 LibreOffice 打开后，应满足：

- 文件名是 `stats_YYYY-MM.xlsx`；
- 每个已完成 UTC 日期对应一个 `YYYY-MM-DD` Sheet；
- 监控开始后的缺失日期保留只有表头的空 Sheet；
- 表头和每日 CSV 一致；
- 同一 `timestamp + card_id` 不重复；
- 当前尚未完成的 UTC 日期不会提前写入。

## 10. 断网排队和恢复补传

正式启动一个灰度 Agent 后停止 Collector 容器：

```bash
docker stop -t 30 npu-monitor-collector
```

等待至少三个采集周期，检查：

```bash
find /work/monitor/runtime-data/agent/spool -name '*.json' | wc -l
cat /work/monitor/runtime-data/agent/upload_health.json
tail -5 /work/monitor/runtime-data/agent/sample_status/samples_$(date -u +%F).jsonl
```

验收条件：

- Agent 容器没有退出；
- 本地 CSV 仍持续追加；
- `spool` 数量随采样增长；
- 上传错误被记录，但不影响下一轮采集。

恢复 Collector：

```bash
docker start npu-monitor-collector
```

等待补传，检查 `spool` 最终归零，Collector 历史中包含断网期间的时间桶，且没有重复记录。

在 Collector 仍不可用时重启 Agent，检查 `upload_health.json`：

- `last_success` 不会变回 `null`；
- `consecutive_failures` 不会因为重启清零；
- `upload_unavailable_since` 保持原值；
- `oldest_pending_at` 和 `oldest_pending_age_seconds` 继续增长；
- 成功补传并清空队列后，`upload_unavailable_since` 才恢复为 `null`。

## 11. 部分采样和失败采样

不要主动断开生产 NPU。删除测试 Agent 容器后，将预期卡数暂时设为 9 并重新创建：

```bash
docker stop -t 30 npu-monitor-agent
docker rm npu-monitor-agent
export NPU_AGENT_EXPECTED_CARDS=9
./deploy/create_agent_container.sh 13315b656180 /work/monitor
```

实际采到 8 张卡时应满足：

- Agent `sample_status=partial`；
- 缺失卡号包含 8；
- 覆盖率为 88.89%；
- Collector 节点状态为 `degraded`；
- 已采到的 8 张卡仍参与集群统计；
- 历史覆盖率分母按 9 张卡计算。

使用不存在的命令重新创建 Agent，可以测试完全失败：

```bash
docker stop -t 30 npu-monitor-agent
docker rm npu-monitor-agent
export NPU_AGENT_NPU_SMI_BIN=/not-exist/npu-smi
./deploy/create_agent_container.sh 13315b656180 /work/monitor
```

预期上报一条 `failed` 采样而不是假装节点离线，Collector 能区分“节点在线但采集失败”和“节点没有上报”。

## 12. stale/offline 状态测试

灰度环境可以在创建 Collector 前临时使用较短阈值：

```bash
docker stop -t 30 npu-monitor-collector
docker rm npu-monitor-collector
export NPU_COLLECTOR_STALE_SECONDS=10
export NPU_COLLECTOR_OFFLINE_SECONDS=25
./deploy/create_collector_container.sh 13315b656180 /work/monitor
```

停止 Agent 后，状态应先从 `online/degraded` 变为 `stale`，再变为 `offline`。实际生效阈值还会取 Agent 采集周期的 2 倍和 5 倍中的较大值；若 Agent 周期为 60 秒，需要分别等待约 120 秒和 300 秒。

## 13. 重启和持久化测试

记录重启前健康信息，然后重启 Collector 容器：

```bash
curl -sS http://主节点IP:18080/health
docker restart -t 30 npu-monitor-collector
curl -sS http://主节点IP:18080/health
```

验收条件：

- `sample_count` 没有归零；
- 已登记节点仍在快照中；
- Agent 在 Collector 重启时产生的队列会自动补传；
- SQLite `database_integrity` 仍为 `ok`。

再重启 Agent，确认旧 `spool` 会先被继续发送，不会因为进程重启丢失。

## 14. 僵尸进程回归测试

确认 mini-init 是容器 PID 1：

```bash
docker exec npu-monitor-agent ps -o pid,ppid,stat,args -p 1
```

PID 1 的命令必须包含 `/work/monitor/deploy/mini_init.py`。

连续启停 20 次：

```bash
for i in $(seq 1 20); do
  docker stop -t 30 npu-monitor-agent || exit 1
  docker start npu-monitor-agent || exit 1
  sleep 2
done
```

检查：

```bash
docker exec npu-monitor-agent sh -c \
  "ps -eo pid,ppid,stat,args | awk '\$3 ~ /^Z/ {print}'"
```

验收条件是没有 `Z` 状态进程。集群 Agent 的 HTTP 上报在线程中运行，不会为发送请求创建新的 Python 进程；`npu-smi` 子进程由 Python `subprocess.run()` 同步等待回收。

## 15. 多节点准确性抽查

先灰度 2 台，每台 8 卡。Console 预期显示：

- 总节点数 2；
- 预期卡数 16；
- 完整采集时活动卡数 16、覆盖率 100%；
- 集群利用率等于 16 张卡当前值的算术平均，而不是两个节点平均值的简单平均；
- HBM 等于所有具有 HBM 数据的卡使用量总和除以容量总和。

人工计算一轮：

```text
集群利用率 = 所有有效卡 utilization 之和 / 有效卡数量
覆盖率 = 实际收到的卡样本数量 / 预期卡样本数量
```

抽查同一 UTC 时间桶的 Agent CSV、Collector 序列输出和 Console，三者数值应一致。

## 16. 24 小时稳定性验收

灰度运行至少 24 小时后检查：

- Agent 和 Collector 容器没有异常重启；
- Agent `spool` 通常为 0；
- `rejected` 没有新文件；
- 每个完整采样都包含 8 卡；
- 集群历史覆盖率符合实际网络和设备状态；
- SQLite 完整性为 `ok`；
- 集群分组、节点搜索分页、单卡历史和原始样本分页结果正确；
- 节点异常会生成 active 告警，恢复后同一告警变为 resolved；
- CSV/XLSX 导出行数和抽样值与 Collector 一致；
- Collector 数据目录和 Agent 数据目录增长符合预期；
- 无僵尸进程；
- 停止任一 Agent 后能正确进入 stale/offline，恢复后自动 online；
- Collector 重启期间数据可补传且不重复。

通过后再逐批扩展到其余机器，建议每批 3–5 台。
